"----------------------------------------------------------
" エンコーディングの設定
if has('vim_starting')
  if !has('nvim')
    " neovimはデフォルトがutfなのでセットしない
    set enc=utf-8
    set fenc=utf-8
  endif
  
  set fileencodings=ucs-bom,utf-8,euc-jp,iso-2022-jp,cp932,utf-16,utf-16le
endif
