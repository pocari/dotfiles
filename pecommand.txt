\ls -F | grep '/$' | xargs -Iarg du -sh arg   # カレントディレクトリのフォルダ単位でdu -shする
git log master..HEAD # masterから派生した今のブランチに対してcommitしたログのみ出力する
tig master..HEAD     # masterから派生した今のブランチに対してcommitしたログのみ出力する
git branch -d $(git branch --merged | grep -v master | grep -v '*') #マージ済みのローカルブランチを削除ただしmasterは除く
docker rmi $(docker images -f dangling=true -q) # 不要なイメージを消す
for i in $(seq 1 10); do time zsh -i -c exit; done # zshの起動時間計る
socat TCP-LISTEN:6000,reuseaddr,fork UNIX-CLIENT:\"$DISPLAY\" # dockerからGUIを受ける口をmac側で開く
vi -c 'call DeinCheckAndInstall()' # deinで未インストールプラグインを一括ダウンロード
rm ~/.zcompdump && compinit -i # zshの補完リセット
git log --since=$(date -v-1d +"%Y/%m/%d") --until=$(date +"%Y/%m/%d") --oneline # 昨日のcommit一覧
